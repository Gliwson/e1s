spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bloodApp?serverTimezone=UTC&characterEncoding=UTF-8&PublicKeyRetrieval=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
  liquibase:
    change-log: classpath:db/changelog/dbchangelog-prod.xml
    enabled: off

server:
  port: 8080

